[project]
name = "training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yolanda", email = "yjian@sparkgeo.com" }
]
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.34",
    "claymodel>=1.5.0",
    "einops>=0.7.0",
    "numpy>=2.4.6",
    "pyyaml>=6.0.3",
    "scipy>=1.18.0",
    "torch>=2.12.1",
    "xarray>=2026.4.0",
]

[build-system]
requires = ["uv_build>=0.11.15,<0.12.0"]
build-backend = "uv_build"
